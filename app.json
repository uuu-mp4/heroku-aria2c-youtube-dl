{
  "name": "youtube-dl",
  "description": "download youtube video",
  "repository": "https://github.com/uuu-mp4/heroku-aria2c-youtube-dl",
  "keywords": ["youtube", "docker", "video"],
  "stack": "container",
  "env": {
    "RCLONE_CONFIG": {
      "description": "Rclone config to mount in one line, replace line breaks with \"\\n\"",
      "required": false
    },
    "RCLONE_DESTINATION": {
      "description": "Path to store your file in your Rclone drive. Must start with a forward slash.",
      "required": false
    }
  }
}
